version: '3.8'

services:
  agent:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./logs:/app/logs
      - ./app/sessions:/app/app/sessions
    env_file:
      - .env
    environment:
      - LOG_LEVEL=INFO
      - AWS_REGION=us-east-1
    restart: unless-stopped
